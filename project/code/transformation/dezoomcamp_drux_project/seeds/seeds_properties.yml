version: 2

seeds: 
  - name: categories_vhl
    config:
      column_types: {code_categ: int64, libelle_categ: varchar(100)}
      delimiter: ','
    description: >
      Catégorie des véhicules impliqués dans des accidents.